import java.util.Random;

public class TabZadanie3 {  /*

    public static int[][] createMatrix(int rows, int columns) {
        return new int[rows][columns];
    }

    public static void fillMatrixwithRandomValues(int[][] matrix) {
        Random random = new Random();
        for (int i = 0; i < matrix.length; i++) {
            for (int j = 0; j < matrix[i].length; j++) {
                matrix[i][j] = random.nextInt();
            }
        }
    }

    static void printMatrix(int[][] matrix) {
        for (int i = 0; i < matrix.length; i++) {
            for (int j = 0; j < matrix[i].length; j++) {
                System.out.println(matrix[i][j] + " |");
            }
        }

        public static void main (String[]args){
            int[][] matrix = createMatrix(5, 5);
            fillMatrixwithRandomValues(matrix);
            printMatrix(matrix);

            int max = Integer.MIN_VALUE;
            int numberOfCols = matrix[0].length;
            System.out.println("Liczba kolumn" + numberOfCols);
            for (int j = 0; j < matrix[0].length; j++){

                if(matrix[0][j] > max){
                    max = matrix[0][j];
                }
            }

        }
    }


*/
}